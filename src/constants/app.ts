/**
 * Application-level configuration constants.
 */

/** Auto-refresh interval for analysis data (ms) */
export const ANALYSIS_POLL_INTERVAL_MS = 15_000;

/** Delay before ranking animation promotes user to #1 (ms) */
export const RANKING_PAUSE_AT_BOTTOM_MS = 2000;

/** Interval for competitor shuffle animation (ms) */
export const RANKING_SHUFFLE_INTERVAL_MS = 1500;
